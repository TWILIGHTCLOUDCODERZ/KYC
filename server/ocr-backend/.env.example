PORT=8080
# Local dev only. In Cloud Run this is injected via --set-secrets from GCP
# Secret Manager (see infra/set-firebase-secret.sh) — never bake it into the
# image or commit it here.
GEMINI_API_KEY=
